import 'bootstrap';
import '../css/app.css'; /* Bootstrap (install via npm) */
import '@fortawesome/fontawesome-free/css/all.min.css'; /* Font Awesome (install via npm) */

/* SweetAlert2 (install via npm) */
import Swal from 'sweetalert2';
window.Swal = Swal;